import {Check,Circle,Clock3,PenLine,Trash2} from 'lucide-react'
import type {Task} from '../domain/types'
export function TaskRow({task,onToggle,onDelete,onEdit,number}:{task:Task;onToggle:(id:number)=>void;onDelete?:(id:number)=>void;onEdit?:(id:number)=>void;number?:number}){return <div className={task.done?'task done':'task'}><button aria-label="切換完成狀態" onClick={()=>onToggle(task.id)}>{task.done?<Check/>:number?<span>{number}</span>:<Circle/>}</button><div><strong>{task.title}</strong><p><span className="task-category">{task.category||'一般'}</span><span>{task.project}</span><i>•</i><span><Clock3 size={13}/>{task.estimate} 分鐘</span>{task.due&&<><i>•</i><span>{task.due}</span></>}</p></div><span className={`energy energy-${task.energy}`}>{task.energy}能量</span>{onEdit&&<button className="row-action" aria-label="編輯任務" onClick={()=>onEdit(task.id)}><PenLine/></button>}{onDelete&&<button className="row-action" aria-label="刪除任務" onClick={()=>onDelete(task.id)}><Trash2/></button>}</div>}
