export type Quadrant='Q1'|'Q2'|'Q3'|'Q4'
export type Energy='高'|'中'|'低'
export type TaskStatus='待辦'|'進行中'|'審核'|'完成'
export type TaskPriority='緊急'|'高'|'中'|'低'
export type Task={id:number;title:string;done:boolean;quadrant:Quadrant;estimate:number;actualMinutes?:number;project:string;category?:string;role?:string;energy:Energy;due?:string;scheduledDay?:number;inbox?:boolean;assignee?:string;priority?:TaskPriority;status?:TaskStatus;progress?:number;reminder?:boolean;nextAction?:string;risk?:string;dependencyId?:number;milestone?:string;captureKind?:string;sourceText?:string}
export type Goal={id:number;title:string;area:string;role?:string;current:number;target:number;unit:string;deadline:string;color:string;reason:string}
export type ProjectCollaborator={email:string;role:'編輯者'|'檢視者';status:'待接受'|'已加入';invitedAt:string}
export type Project={id:number;title:string;area:string;role?:string;progress:number;deadline:string;status:'進行中'|'規劃中'|'暫停'|'完成';color:string;description?:string;goal?:string;owner?:string;startDate?:string;weeklyCapacity?:number;risk?:string;completedAt?:string;archivedAt?:string;retrospective?:string;collaborators?:ProjectCollaborator[]}
export type Habit={id:number;name:string;category:string;streak:number;days:boolean[]}
export type JournalEntry={id:number;title:string;type:string;content:string;mood:number;date:string}
export type Note={id:number;title:string;content:string;tag:string;updated:string}
export type Health={water:number;sleep:number;energy:number;stress:number;mood:number;exercise:number}
export type WorkProfile={id:number;name:string;organization:string;role:string;type:string;rate:number;unit:string;status:'進行中'|'暫停'}
export type WorkLog={id:number;workId:number;date:string;duration:number;task:string;income:number;paid:boolean}
export type Exam={id:number;name:string;type:string;date:string;current:number;target:number;status:string;subjects:{name:string;progress:number;confidence:number}[]}
export type MockExam={id:number;examId:number;date:string;score:number;note:string}
export type Account={id:number;name:string;type:string;balance:number;color:string}
export type FinanceTransaction={id:number;accountId:number;type:'收入'|'支出';category:string;amount:number;date:string;note:string}
export type Budget={category:string;limit:number;color:string}
export type Holding={id:number;symbol:string;name:string;type:string;quantity:number;avgPrice:number;currentPrice:number;color:string}
export type InvestmentTransaction={id:number;holdingId:number;action:'買入'|'賣出';quantity:number;price:number;date:string}
export type Profile={name:string;headline:string;roles:string[];lifeAreas:string[]}
export type FormKind='goal'|'project'|'work'|'exam'|'transaction'|'account'|'holding'|'note'|'journal'|'habit'|'waiting'|'someday'|'study'|'mistake'|'exercise'|'sleep'|'body'|'symptom'|'recovery'|'event'|null
export type WaitingItem={id:number;title:string;kind:'waiting'|'someday';followUp?:string;note:string;done:boolean}
export type FocusKind='task'|'project'|'learning'|'work'|'custom'
export type FocusLog={id:number;taskId?:number;kind?:FocusKind;entityId?:number;context?:string;title:string;minutes:number;quality:number;interruptions:string[];date:string}
export type StudyLog={id:number;examId:number;subject:string;minutes:number;questions:number;correct:number;date:string}
export type Mistake={id:number;examId:number;subject:string;source:string;reason:string;concept:string;reviewed:boolean}
export type ExerciseLog={id:number;type:string;duration:number;intensity:string;date:string}
export type SleepLog={id:number;hours:number;quality:number;date:string}
export type BodyLog={id:number;weight:number;bodyFat:number;waist:number;date:string}
export type WellnessLog={id:number;kind:'symptom'|'recovery';name:string;note:string;minutes?:number;date:string}
export type CalendarEvent={id:number;title:string;date:string;start:string;end:string;category:string;recurrence:'無'|'每天'|'每週'|'每月';recurrenceEnd?:string;note:string}
export type FriendFocus={id:number;name:string;activity:string;category:string;minutes:number;weeklyMinutes:number;streak:number;online:boolean;privacy:'focus'|'category'|'detail'}
export type FocusGroup={id:number;name:string;description:string;members:number;online:number;weeklyMinutes:number;joined:boolean}
export type SocialChallenge={id:number;title:string;description:string;target:number;unit:'分鐘'|'次'|'天';participants:number;joined:boolean;progress:number}
